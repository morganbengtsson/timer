#ifndef TIMER_TIMER_HPP
#define TIMER_TIMER_HPP

#include <chrono>
#include <functional>
#include <async>

class Timer {
public:
  void call(std::function<void>())
private:

};

#endif //TIMER_TIMER_HPP
