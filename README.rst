A crude implementation of a periodic timer in C++.

Clone the repository with included dependencies:

.. code-block:: bash

  git clone --recursive https://github.com/morganbengtsson/timer.git


Create build files by running CMake.